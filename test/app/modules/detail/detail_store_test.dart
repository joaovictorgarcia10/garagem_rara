import 'package:flutter_modular_test/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:desafio_raro/app/modules/detail/detail_store.dart';
import 'package:desafio_raro/app/modules/detail/detail_module.dart';

void main() {
  initModule(DetailModule());
  // DetailStore detail;
  //
  setUp(() {
    //     detail = DetailModule.to.get<DetailStore>();
  });

  group('DetailStore Test', () {
    //   test("First Test", () {
    //     expect(detail, isInstanceOf<DetailStore>());
    //   });

    //   test("Set Value", () {
    //     expect(detail.value, equals(0));
    //     detail.increment();
    //     expect(detail.value, equals(1));
    //   });
  });
}
